spring:
 datasource:
   url: jdbc:mysql://localhost:3306/PatientAuthMySQL
   username: root
   password: root
 jpa:
   hibernate:
     ddl-auto: update
   show-sql: true
   properties:
      hibernate:
          dialect: org.hibernate.dialect.MySQL5Dialect
   
   
   
   
   
